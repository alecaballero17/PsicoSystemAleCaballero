name: psicosystem_mobile
description: "App Móvil de PsicoSystem (Portal de Pacientes)"
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0
  flutter_dotenv: ^6.0.0
  firebase_core: ^2.24.2
  firebase_messaging: ^14.7.10
  qr_flutter: ^4.1.0
  intl: ^0.18.1
  google_fonts: ^6.1.0
  provider: ^6.1.2
  record: ^6.2.1
  permission_handler: ^11.3.1
  path_provider: ^2.1.3
  open_filex: ^4.4.0
  url_launcher: ^6.2.5
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - .env # <--- AGREGAR ESTA SECCIÓN PARA LEER EL ARCHIVO
